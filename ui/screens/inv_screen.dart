import 'package:flutter/material.dart';

class InvScreen extends StatelessWidget {
  const InvScreen({super.key});

  @override
  Widget build(BuildContext context) => const Center(child: Text('Inv'));
}
