class HeroLayers {
  /// Per ora sono “keys” (non path). Quando metti asset veri, qui puoi mettere path.
  final String headKey;
  final String armorKey;
  final String weaponKey;

  const HeroLayers({
    required this.headKey,
    required this.armorKey,
    required this.weaponKey,
  });
}
